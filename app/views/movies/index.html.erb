<div class="navbar navbar-inverse">
	<div class="navbar-inner">
		<div class="container">
			<div class="brandNoHover">Movies</div>
			<ul class="nav">
				<% if user_signed_in? %>
					<% if current_user.has_role? :admin %>
						<%= link_to raw('<i class="icon-plus"></i>'), new_movie_path, :class => "btn btn-primary" %>
					<%end%>
				<%end%>
			</ul>
		</div>
	</div>
</div>

<div class="movieScrollPane">
	<%= render @movies %>
</div>










		

	

  		
